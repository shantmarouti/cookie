{
  "name": "cookie",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npm run build && mocha dist/tests/**/*.tests.js",
    "build": "tsc",
    "lint": "tslint --fix -p ./tsconfig.json -c ./tslint.json -e dist/**/*.ts",
    "format": "prettier --write --config ./.prettierrc.json --ignore-path ./.gitignore \"{src,tests}/**/*\"",
    "precommit": "npm run format && npm run lint && npm run test"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^4.1.2",
    "@types/mocha": "^2.2.48",
    "chai": "^4.1.2",
    "mocha": "^5.0.1",
    "prettier": "^1.10.2",
    "tslint": "^5.9.1",
    "tslint-language-service": "^0.9.8",
    "typescript": "^2.7.1"
  },
  "peerDependencies": {
    "tslib": "^1.9.0"
  }
}
